using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pop_small : MonoBehaviour
{
    [SerializeField]
    private GameObject pop_up;

    void Start()
    {
        pop_up.SetActive(true);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
